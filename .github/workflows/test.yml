name: ci

on: [ push, pull_request ]

jobs:
  msys2-gcc:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v2
      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: base-devel git autoconf automake libtool pkg-config flex bison mingw-w64-x86_64-check mingw-w64-x86_64-toolchain
      - name: submodule update
        run: git submodule update --init --recursive --remote
      - name: autoconf/automake
        run: ./autogen.sh
      - name: configure
        run: ./configure --without-libxml2 --disable-static --enable-shared CFLAGS=-Wno-array-bounds CPPFLAGS="-D __SCNLg=\\\".17g\\\" -D __SCNi8=SCNi16 -D __SCNu8=SCNu16"
      - name: patch libtool
        run: sed -i.bak -e "s/\(allow_undefined=\)yes/\1no/" libtool
      - name: make
        run: make -j 2
      - name: dump
        run: ./examples/type_prefixes

  msys2-clang:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v2
      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: base-devel git autoconf automake libtool pkg-config flex bison mingw-w64-x86_64-check mingw-w64-x86_64-toolchain
      - name: submodule update
        run: git submodule update --init --recursive --remote
      - name: autoconf/automake
        run: ./autogen.sh
      - name: patch Makefile.am
        run: sed -i.bak -e "s/test_dwarfdumpPE.sh test_dwarfdumpMacos.sh//" libdwarf-code/test/Makefile.am
      - name: configure
        run: ./configure --without-libxml2 --disable-static --enable-shared CC=clang
      - name: make
        run: make -j 2
      - name: dump
        run: ./examples/type_prefixes

