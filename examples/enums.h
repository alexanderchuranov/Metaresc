
#include <reslib.h>

TYPEDEF_ENUM (color_t,
              (BLACK),        // becomes 0
              (RED,     = 1),
              (GREEN,      ), // becomes 2
              (YELLOW),       // becomes 3
              (BLUE,    = 4),
              (MAGENTA, = 5),
              (CYAN,    = 6),
              (WHITE,   = 7)
              )
